export const separateNumbers = (numbers: number): string => {
  return numbers.toLocaleString("fa-IR");
};
export const separateNumbersEn = (numbers: number): string => {
  return numbers.toLocaleString("en-US");
};